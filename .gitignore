.DS_Store
# never commit dumps, logs, or secrets
*.sql
*.sql.gz
migration-log/
.env
.env.*
# Swift / Xcode
app/.build/
app/*.xcodeproj/xcuserdata/
app/DerivedData/
*.xcuserstate
# Valet machine-local state never lives here
Nginx/
Certificates/

# menu-bar app build output
app/build/

# Machine-local, never public: the real site inventory and the internal migration runbooks
sites.local.tsv
docs/private/
